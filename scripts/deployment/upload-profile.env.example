# Keep the real profile outside Git when it contains internal hostnames.

DEPLOY_REMOTE_HOST=skills.example.com
DEPLOY_REMOTE_USER=deploy
DEPLOY_REMOTE_DIR=/srv/managed-skill-hub
# DEPLOY_SSH_PORT=22
